/// <summary>
/// Интерфейс для модулей, которые должны сохраняться между сценами
/// Модули без этого интерфейса будут пересоздаваться в каждой сцене
/// </summary>
public interface IPersistentModule : IGameModule
{
    // Маркерный интерфейс - дополнительных методов не требует
    // Указывает, что модуль должен оставаться между сценами
}
